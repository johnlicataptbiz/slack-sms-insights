# Let npm auto-detect workspace mode from cwd/package context.
# Forcing workspaces globally breaks `npm config get cache` (used by setup-node).
save-workspace-protocol=rolling

# Enable package-lock deduplication
package-lock=true

# Fund messages off (less noise in CI)
fund=false

# Audit level (fail on high/critical only)
audit-level=high
