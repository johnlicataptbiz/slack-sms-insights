# Skeleton UI + Error Retry Implementation

## Tasks

- [x] 1. Update `V2Primitives.tsx` — add `onRetry` prop to `V2State` with "Try Again" + "Reload Page" buttons
- [x] 2. Update `InsightsV2.tsx` — replace loading spinner with `SkeletonDashboard`, wire retry
- [x] 3. Update `RunsV2.tsx` — replace loading spinner with `SkeletonTable`, wire retry
- [x] 4. Update `RepV2.tsx` — replace loading spinner with `SkeletonDashboard`, wire retry
- [x] 5. Update `SequencesV2.tsx` — replace loading spinner with `SkeletonDashboard`, wire retry
- [x] 6. Update `InboxV2.tsx` — replace detail loading spinner with `SkeletonText`, wire retry on list + detail errors
- [x] 7. Update `components.css` — add `.V2State__actions` retry button styles

## All tasks complete ✓
