{
  "name": "slack-sms-insights",
  "private": true,
  "workspaces": [
    "apps/frontend",
    "apps/backend"
  ],
  "scripts": {
    "dev": "concurrently \"npm --prefix apps/backend run dev\" \"npm --prefix apps/frontend run dev\"",
    "dev:backend": "npm --prefix apps/backend run dev",
    "dev:frontend": "npm --prefix apps/frontend run dev",
    "build": "npm --prefix apps/backend run build && npm --prefix apps/frontend run build",
    "build:backend": "npm --prefix apps/backend run build",
    "build:frontend": "npm --prefix apps/frontend run build",
    "lint": "npm --prefix apps/backend run lint && npm --prefix apps/frontend run lint",
    "lint:backend": "npm --prefix apps/backend run lint",
    "lint:frontend": "npm --prefix apps/frontend run lint",
    "test": "npm --prefix apps/backend run test && npm --prefix apps/frontend run test",
    "typecheck": "npm --prefix apps/frontend run typecheck:v2",
    "typecheck:v2": "npm --prefix apps/frontend run typecheck:v2"
  },
  "devDependencies": {
    "concurrently": "^8.2.2",
    "typescript": "^5.9.3",
    "vitest": "^4.1.0",
    "@biomejs/biome": "^2.4.2",
    "@types/node": "^25.3.0"
  }
}
